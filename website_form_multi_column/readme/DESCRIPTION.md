This module adds a "Columns" option to the website Form snippet.

The website editor can split the form into 2, 3 or 4 columns and drag any
field between them. Column layout is saved to the page like any other
website edit; the form submission flow is unchanged.
