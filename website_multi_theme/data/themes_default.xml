<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright 2018 Ivan Yelizariev <https://it-projects.info/team/yelizariev>
     License LGPL-3.0 or later (http://www.gnu.org/licenses/lgpl.html). -->
<odoo>
    <!-- Default theme. It's basically a workaround for "Multi-customization"
         feature which requires theme be set -->
    <record model="website.theme" id="theme_default">
        <field name="name">Default Theme</field>
    </record>

    <template id="theme_default_dummy" name="Dummy asset for Default Theme">
        <!-- It's empty, but we have to have one, because otherwise multi-theme
             magic is not applied for themes without any views -->
    </template>


    <record model="website.theme.asset" id="theme_default_asset">
        <field name="name">website_multi_theme.theme_default_dummy</field>
        <field name="theme_id" ref="theme_default"/>
    </record>

    <!-- This method prepare assets and applies default theme for all websites -->
    <function model="website.config.settings"
              name="multi_theme_reload"
              />
</odoo>
